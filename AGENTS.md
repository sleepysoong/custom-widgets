# 저장소 작업 지침

## 작업 완료 시 커밋 및 푸시

- 사용자가 요청한 코드·문서 작업이 완료되면 같은 작업 흐름에서 변경 사항을 커밋하고 현재 작업 브랜치를 `origin`에 푸시한다. 별도 요청을 기다리지 않는다.
- 커밋 전에 `git status`와 diff를 확인하고, 해당 작업과 무관한 사용자 변경은 스테이징하거나 커밋하지 않는다.
- `git diff --check`로 공백 오류를 확인하고, 변경 의도가 드러나는 커밋 메시지를 사용한다.
- 푸시 후 원격 브랜치와 로컬 상태를 확인한다. 자동화가 원격 브랜치를 추가로 갱신했다면 안전한 fast-forward 방식으로 로컬을 동기화한다.
- 테스트와 빌드는 사용자의 요청 및 현재 작업 지시를 따른다. 사용자가 실행하지 말라고 한 검증은 수행하지 않는다.
